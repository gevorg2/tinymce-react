import * as fc from 'fast-check';
import { TestLabel } from '../../main/ts/api/TestLabel';
import { describe, it } from 'mocha';
describe("TestLabel.asString", function () {
    it("stringifies strings", function () {
        fc.assert(fc.property(fc.string(), function (s) { return TestLabel.asString(s) === s; }));
    });
    it("stringifies () => strings", function () {
        fc.assert(fc.property(fc.string(), function (s) { return TestLabel.asString(function () { return s; }) === s; }));
    });
});
describe("TestLabel.asStringOr", function () {
    it("stringifies strings", function () {
        fc.assert(fc.property(fc.string(), fc.string(), function (a, b) { return TestLabel.asStringOr(a, b) === a; }));
    });
    it("defaults for null", function () {
        fc.assert(fc.property(fc.string(), function (a) { return TestLabel.asStringOr(null, a) === a; }));
    });
    it("defaults for undefined", function () {
        fc.assert(fc.property(fc.string(), function (a) { return TestLabel.asStringOr(undefined, a) === a; }));
    });
});
//# sourceMappingURL=TestLabelTest.js.map