import * as fc from 'fast-check';
import { assert } from 'chai';
import { UrlParams } from '../../main/ts/core/UrlParams';
import { describe, it } from 'mocha';
describe("UrlParams.nat", function () {
    it("parses undefined as 0", function () {
        assert.equal(UrlParams.nat(undefined), 0);
    });
    it("parses a nat as a nat", function () {
        fc.assert(fc.property(fc.nat(), function (n) { return UrlParams.nat(String(n)) === n; }));
    });
    it("parses a neg as 0", function () {
        fc.assert(fc.property(fc.nat(), function (n) { return UrlParams.nat(String(-n)) === 0; }));
    });
    it("parses a non-number as 0", function () {
        assert.equal(UrlParams.nat(''), 0);
        assert.equal(UrlParams.nat('cat'), 0);
        assert.equal(UrlParams.nat('[]'), 0);
        assert.equal(UrlParams.nat('frog'), 0);
    });
});
//# sourceMappingURL=UrlParamsTest.js.map