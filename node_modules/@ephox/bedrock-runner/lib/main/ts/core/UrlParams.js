import * as QS from 'querystringify';
var nat = function (str) {
    if (typeof str === 'string') {
        var num = parseInt(str, 10);
        if (!isNaN(num) && num > 0) {
            return num;
        }
    }
    return 0;
};
var parse = function (search, makeSessionId) {
    var params = QS.parse(search);
    return {
        session: params.session || makeSessionId(),
        offset: nat(params.offset),
        failed: nat(params.failed),
        retry: nat(params.retry),
    };
};
export var UrlParams = {
    parse: parse,
    nat: nat
};
//# sourceMappingURL=UrlParams.js.map