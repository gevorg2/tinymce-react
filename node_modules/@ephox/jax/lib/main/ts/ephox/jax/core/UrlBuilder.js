import { Strings, Obj } from '@ephox/katamari';
export var buildUrl = function (url, queryParams) {
    return queryParams.map(function (query) {
        var parts = Obj.mapToArray(query, function (v, k) { return encodeURIComponent(k) + '=' + encodeURIComponent(v); });
        var prefix = Strings.contains(url, '?') ? '&' : '?';
        return parts.length > 0 ? url + prefix + parts.join('&') : url;
    }).getOr(url);
};
//# sourceMappingURL=UrlBuilder.js.map