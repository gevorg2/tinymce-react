import { Future } from '@ephox/katamari';
import { FileReader, Blob } from '@ephox/dom-globals';
var readBlobAsText = function (blob) { return Future.nu(function (callback) {
    var fr = new FileReader();
    fr.onload = function (e) {
        var data = e.target ? e.target.result : new Blob([]);
        callback(data);
    };
    fr.readAsText(blob);
}); };
export { readBlobAsText };
//# sourceMappingURL=BlobReader.js.map