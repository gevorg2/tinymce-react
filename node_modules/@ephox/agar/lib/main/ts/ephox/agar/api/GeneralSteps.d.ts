import { Step } from './Step';
declare const sequence: (steps: Step<any, any>[]) => Step<any, any>;
declare const repeat: <T>(amount: number, step: Step<T, T>) => Step<T, T>[];
declare const sequenceRepeat: <T>(amount: number, step: Step<T, T>) => Step<T, T>;
declare const repeatUntil: <T, U>(label: string, repeatStep: Step<T, T>, successStep: Step<T, U>, numAttempts: number) => Step<T, U>;
declare const waitForPredicate: <T>(label: string, interval: number, amount: number, predicate: () => boolean) => Step<T, T>;
export { sequence, repeatUntil, waitForPredicate, repeat, sequenceRepeat };
//# sourceMappingURL=GeneralSteps.d.ts.map