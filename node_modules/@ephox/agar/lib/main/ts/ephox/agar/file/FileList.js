import { Arr } from '@ephox/katamari';
var createFileList = function (inputFiles) {
    var files = {
        length: inputFiles.length,
        item: function (idx) {
            return inputFiles[idx];
        }
    };
    Arr.each(inputFiles, function (file, idx) {
        files[idx] = file;
    });
    return Object.freeze(files);
};
export { createFileList };
//# sourceMappingURL=FileList.js.map