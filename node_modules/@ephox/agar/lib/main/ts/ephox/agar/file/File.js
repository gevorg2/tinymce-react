import { Blob } from '@ephox/dom-globals';
var createFile = function (name, lastModified, blob) {
    var newBlob = new Blob([blob], { type: blob.type });
    newBlob.name = name;
    newBlob.lastModified = lastModified;
    return Object.freeze(newBlob);
};
var createFileFromString = function (name, lastModified, text, mime) {
    return createFile(name, lastModified, new Blob([text], { type: mime }));
};
export { createFile, createFileFromString };
//# sourceMappingURL=File.js.map