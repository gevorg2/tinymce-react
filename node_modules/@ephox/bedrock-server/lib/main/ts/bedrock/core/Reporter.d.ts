import { TestResults, TestResult } from '../server/Controller';
import { Attempt } from './Attempt';
export interface ReporterSettings {
    name: string;
    output: string;
}
/**
 * XML has no way of "escaping" the CDATA end token ("]]>"), so we split into several CDATA sections
 * in the middle of this token. Jenkins seems fine with multiple CDATA sections within a <failure> tag.
 * @param s
 */
export declare const splitCdatas: (s: string) => string[];
export declare const write: (settings: ReporterSettings) => (data: TestResults) => Promise<Attempt<string[], TestResult[]>>;
export declare const writePollExit: (settings: ReporterSettings, results: TestResults) => Promise<never>;
