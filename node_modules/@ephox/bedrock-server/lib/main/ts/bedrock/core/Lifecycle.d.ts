import { BrowserObject } from 'webdriverio';
import { TestResult } from '../server/Controller';
import { Attempt } from './Attempt';
export declare const shutdown: (promise: Promise<Attempt<string[], TestResult[]>>, driver: BrowserObject, done: () => Promise<any>, gruntDone: ((success: boolean) => void) | null, delayExiting: boolean) => Promise<void>;
