import { BrowserObject, Element } from 'webdriverio';
export declare type ElementWithActions = Element & {
    performActions: (actions: Array<Record<string, any>>) => Promise<void>;
    releaseActions: () => Promise<void>;
};
export declare const getTarget: (driver: BrowserObject, data: {
    selector: string;
}) => Promise<ElementWithActions>;
export declare const performActionOnTarget: <T>(driver: BrowserObject, data: {
    selector: string;
}, action: (target: ElementWithActions) => Promise<T>) => Promise<T>;
