import { BrowserObject } from 'webdriverio';
import * as Routes from './Routes';
import * as Controller from './Controller';
import { Attempt } from '../core/Attempt';
import { DriverMaster } from './DriverMaster';
export declare const create: (master: DriverMaster, maybeDriver: Attempt<any, BrowserObject>, projectdir: string, basedir: string, stickyFirstSession: boolean, singleTimeout: number, overallTimeout: number, testfiles: string[], loglevel: "simple" | "advanced", resetMousePosition: boolean) => {
    routers: Routes.Route[];
    markLoaded: () => void;
    enableHud: () => void;
    awaitDone: () => Promise<Controller.TestResults>;
};
