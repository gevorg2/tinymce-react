import { BrowserObject } from 'webdriverio';
interface KeyCombo {
    key: string;
    altKey?: boolean;
    ctrlKey?: boolean;
    metaKey?: boolean;
    shiftKey: boolean;
}
declare type KeyTextItem = {
    text: string;
};
declare type KeyComboItem = {
    combo: KeyCombo;
};
declare type KeyItem = KeyTextItem | KeyComboItem;
export interface KeyData {
    keys: KeyItem[];
    selector: string;
}
export declare const executor: (driver: BrowserObject) => (data: KeyData) => Promise<void>;
export {};
