declare type Executor<T> = () => Promise<T>;
export interface DriverMaster {
    waitForIdle: <T>(f: Executor<T>, label: string) => Promise<T>;
}
export declare const create: () => {
    waitForIdle: <T>(f: Executor<T>, label: string) => Promise<T>;
};
export {};
