import { BrowserObject } from 'webdriverio';
export interface MouseData {
    type: 'move' | 'click' | 'down' | 'up';
    selector: string;
}
export declare const executor: (driver: BrowserObject) => (data: MouseData) => Promise<void>;
