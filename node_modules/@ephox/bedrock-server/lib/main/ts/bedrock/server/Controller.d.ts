export interface TestResult {
    name: string;
    file: string;
    passed: boolean;
    time: string;
    skipped?: boolean;
    error: string;
}
export interface TestResults {
    message?: string;
    results: TestResult[];
    start: number;
    now: number;
}
export declare const create: (stickyFirstSession: boolean, singleTimeout: number, overallTimeout: number, testfiles: string[], loglevel: "simple" | "advanced") => {
    enableHud: () => void;
    recordAlive: (sessionId: string) => void;
    recordTestStart: (id: string, name: string, file: string, totalTests: number) => void;
    recordTestResult: (id: string, name: string, file: string, passed: boolean, time: string, error: string) => void;
    recordDone: (id: string) => void;
    awaitDone: () => Promise<TestResults>;
};
