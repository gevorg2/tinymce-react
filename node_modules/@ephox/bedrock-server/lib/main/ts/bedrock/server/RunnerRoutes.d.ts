import * as Routes from './Routes';
interface Runner {
    routers: Routes.Route[];
    fallback: Routes.Route;
}
export declare const generate: (mode: string, projectdir: string, basedir: string, configFile: string, bundler: "webpack" | "rollup", testfiles: string[], chunk: number, retries: number, singleTimeout: number, stopOnFailure: boolean, basePage: string, coverage: string[]) => Promise<Runner>;
export {};
