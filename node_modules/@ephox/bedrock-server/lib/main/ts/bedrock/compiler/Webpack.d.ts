import * as Serve from '../server/Serve';
export interface WebpackServeSettings extends Serve.ServeSettings {
    config: string;
    coverage: string[];
}
export declare const compile: (tsConfigFile: string, scratchDir: string, basedir: string, exitOnCompileError: boolean, srcFiles: string[], coverage: string[]) => Promise<string>;
export declare const devserver: (settings: WebpackServeSettings) => Promise<Serve.ServeService>;
