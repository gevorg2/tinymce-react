"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const fs = require("fs");
const Webpack = require("../compiler/Webpack");
exports.compile = (tsConfigFile, scratchDir, basedir, exitOnCompileError, files, coverage) => {
    const getCompileFunc = () => {
        return Webpack.compile;
    };
    const generate = () => {
        const compile = getCompileFunc();
        return compile(tsConfigFile, scratchDir, basedir, exitOnCompileError, files, coverage).then((compiledJsFilePath) => fs.readFileSync(compiledJsFilePath));
    };
    return {
        generate
    };
};
//# sourceMappingURL=Compiler.js.map