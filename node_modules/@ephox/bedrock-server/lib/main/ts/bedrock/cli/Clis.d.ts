import * as cli from './Cli';
import { Attempt } from '../core/Attempt';
import { BedrockAutoSettings, BedrockFrameworkSettings, BedrockSettings } from '../core/Settings';
export interface Directories {
    current: string;
    bin: string;
}
export declare const forAuto: (directories: Directories, argv?: string[]) => Attempt<cli.CliError, BedrockAutoSettings>;
export declare const forManual: (directories: Directories, argv?: string[]) => Attempt<cli.CliError, BedrockSettings>;
export declare const forFramework: (directories: Directories, argv?: string[]) => Attempt<cli.CliError, BedrockFrameworkSettings>;
export declare const logAndExit: (errs: cli.CliError) => never;
