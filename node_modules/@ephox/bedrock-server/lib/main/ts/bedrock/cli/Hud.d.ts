declare type ResultData = {
    done: boolean;
    id: string;
    numFailed: number;
    numPassed: number;
    test?: string;
    totalFiles?: number;
    totalTests?: number;
};
export declare const create: (testfiles: string[], loglevel: "simple" | "advanced") => {
    update: (data: ResultData) => Promise<{}>;
    complete: () => Promise<{}>;
};
export {};
