import * as commandLineArgs from 'command-line-args';
import { Attempt } from '../core/Attempt';
import { ClOption } from './ClOptions';
export declare type CliError = {
    command: string;
    errors: string[];
    usage: string;
};
export declare const extract: (command: string, desc: string, definitions: ClOption[], argv: string[]) => Attempt<CliError, commandLineArgs.CommandLineOptions>;
