import { BedrockSettings } from './bedrock/core/Settings';
declare type Program = {
    go: (settings: BedrockSettings, directories: {
        current: string;
        bin: string;
    }) => void;
    mode: 'forAuto' | 'forManual' | 'forFramework';
};
export declare const run: (program: Program, directories: {
    current: string;
    bin: string;
}) => void;
export {};
