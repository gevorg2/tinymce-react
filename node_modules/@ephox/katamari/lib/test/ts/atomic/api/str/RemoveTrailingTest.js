import * as Strings from 'ephox/katamari/api/Strings';
import { UnitTest, Assert } from '@ephox/bedrock-client';
import fc from 'fast-check';
UnitTest.test('removeTrailing: unit tests', function () {
    function check(expected, str, trail) {
        var actual = Strings.removeTrailing(str, trail);
        Assert.eq('removeTrailing', expected, actual);
    }
    check('', '', '');
    check('cat', 'cat', '');
    check('', '', '/');
    check('cat', 'cat/', '/');
    check('', 'cat/', 'cat/');
});
UnitTest.test('removeTrailing property', function () {
    fc.assert(fc.property(fc.asciiString(), fc.asciiString(), function (prefix, suffix) {
        Assert.eq('removeTrailing', prefix, Strings.removeTrailing(prefix + suffix, suffix));
    }));
});
//# sourceMappingURL=RemoveTrailingTest.js.map