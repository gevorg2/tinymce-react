import * as Arr from 'ephox/katamari/api/Arr';
import { Assert, UnitTest } from '@ephox/bedrock-client';
import { Option } from 'ephox/katamari/api/Option';
import { tOption } from 'ephox/katamari/api/OptionInstances';
import { Testable } from '@ephox/dispute';
import fc from 'fast-check';
var tNumber = Testable.tNumber;
UnitTest.test('Arr.last: empty', function () {
    Assert.eq('empty', Option.none(), Arr.last([]), tOption(tNumber));
});
UnitTest.test('Arr.last: nonEmpty', function () {
    fc.assert(fc.property(fc.array(fc.integer()), fc.integer(), function (init, last) {
        var arr = init.concat([last]);
        Assert.eq('nonEmpty', Option.some(last), Arr.last(arr), tOption(tNumber));
    }));
});
//# sourceMappingURL=ArrLastTest.js.map