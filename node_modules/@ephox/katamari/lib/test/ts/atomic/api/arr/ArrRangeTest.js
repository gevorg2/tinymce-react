import * as Arr from 'ephox/katamari/api/Arr';
import * as Fun from 'ephox/katamari/api/Fun';
import { UnitTest, Assert } from '@ephox/bedrock-client';
import * as fc from 'fast-check';
import { Testable } from '@ephox/dispute';
var tArray = Testable.tArray, tNumber = Testable.tNumber;
UnitTest.test('Arr.range: unit tests', function () {
    var check = function (expected, input, f) {
        var actual = Arr.range(input, f);
        Assert.eq('range', expected, actual, tArray(tNumber));
    };
    check([], 0, Fun.constant(10));
    check([10], 1, Fun.constant(10));
    check([10, 20, 30], 3, function (x) { return 10 * (x + 1); });
});
UnitTest.test('Arr.range: property tests', function () {
    fc.assert(fc.property(fc.nat(30), function (num) {
        var range = Arr.range(num, Fun.identity);
        Assert.eq('length', range.length, num, tNumber);
        return Arr.forall(range, function (x, i) { return x === i; });
    }));
});
//# sourceMappingURL=ArrRangeTest.js.map