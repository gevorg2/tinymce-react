import * as Id from 'ephox/katamari/api/Id';
import { UnitTest, Assert } from '@ephox/bedrock-client';
import fc from 'fast-check';
UnitTest.test('Id: Unit Tests', function () {
    var one = Id.generate('test');
    var two = Id.generate('test');
    Assert.eq('id has prefix #1', 0, one.indexOf('test'));
    Assert.eq('id has prefix #1', 0, two.indexOf('test'));
    Assert.eq('subsequent ids are inequal', false, one === two);
});
UnitTest.test('Id: Two ids should not be the same', function () {
    var arbId = fc.string(1, 30).map(Id.generate);
    fc.assert(fc.property(arbId, arbId, function (id1, id2) { return id1 !== id2; }));
});
//# sourceMappingURL=IdTest.js.map