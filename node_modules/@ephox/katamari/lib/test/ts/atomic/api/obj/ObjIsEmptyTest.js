import * as Obj from 'ephox/katamari/api/Obj';
import fc from 'fast-check';
import { UnitTest, Assert } from '@ephox/bedrock-client';
UnitTest.test('Obj.isEmpty: unit tests', function () {
    Assert.eq('eq', true, Obj.isEmpty({}));
    Assert.eq('eq', false, Obj.isEmpty({ cat: 'dog' }));
});
UnitTest.test('Obj.isEmpty: single key/value', function () {
    fc.assert(fc.property(fc.string(), fc.json(), function (k, v) {
        var _a;
        var o = (_a = {}, _a[k] = v, _a);
        Assert.eq('eq', false, Obj.isEmpty(o));
    }));
});
//# sourceMappingURL=ObjIsEmptyTest.js.map