import { Assert, UnitTest } from '@ephox/bedrock-client';
import * as Options from 'ephox/katamari/api/Options';
import { Option } from 'ephox/katamari/api/Option';
import { tOption } from 'ephox/katamari/api/OptionInstances';
import * as Fun from 'ephox/katamari/api/Fun';
UnitTest.test('Options.mapFrom', function () {
    Assert.eq('eq', 4, Options.mapFrom(3, function (x) { return x + 1; }).getOrDie());
    Assert.eq('eq', Option.none(), Options.mapFrom(null, Fun.die('boom')), tOption());
    Assert.eq('eq', Option.none(), Options.mapFrom(undefined, Fun.die('boom')), tOption());
});
UnitTest.test('Options.mapFrom === Options.map().from()', function () {
    var f = function (x) { return x + 1; };
    var check = function (input) {
        Assert.eq('eq', true, Options.mapFrom(input, f).equals(Option.from(input).map(f)));
    };
    check(3);
    check(null);
    check(undefined);
});
//# sourceMappingURL=OptionsMapFromTest.js.map