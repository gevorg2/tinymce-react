import * as Obj from 'ephox/katamari/api/Obj';
import { UnitTest, Assert } from '@ephox/bedrock-client';
UnitTest.test('Obj.values', function () {
    var check = function (expValues, input) {
        var c = function (expected, v) {
            v.sort();
            Assert.eq('values', expected, v);
        };
        c(expValues, Obj.values(input));
    };
    check([], {});
    check(['A'], { a: 'A' });
    check(['A', 'B', 'C'], { a: 'A', c: 'C', b: 'B' });
});
//# sourceMappingURL=ObjValuesTest.js.map