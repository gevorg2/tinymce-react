import * as Arr from 'ephox/katamari/api/Arr';
import { Option } from 'ephox/katamari/api/Option';
import { tOption } from 'ephox/katamari/api/OptionInstances';
import { Assert, UnitTest } from '@ephox/bedrock-client';
import fc from 'fast-check';
import { Testable } from '@ephox/dispute';
var tNumber = Testable.tNumber;
UnitTest.test('Arr.head: empty', function () {
    Assert.eq('empty', Option.none(), Arr.head([]), tOption(tNumber));
});
UnitTest.test('Arr.head: nonEmpty', function () {
    fc.assert(fc.property(fc.array(fc.integer()), fc.integer(), function (t, h) {
        var arr = [h].concat(t);
        Assert.eq('nonEmpty', Option.some(h), Arr.head(arr), tOption(tNumber));
    }));
});
//# sourceMappingURL=ArrHeadTest.js.map