import * as Strings from 'ephox/katamari/api/Strings';
import { UnitTest, Assert } from '@ephox/bedrock-client';
import fc from 'fast-check';
UnitTest.test('ensureLeading', function () {
    function check(expected, str, prefix) {
        var actual = Strings.ensureLeading(str, prefix);
        Assert.eq('ensureLeading', expected, actual);
    }
    check('', '', '');
    check('a', 'a', 'a');
    check('ab', 'ab', 'a');
    check('ab', 'b', 'a');
    check('a', '', 'a');
});
UnitTest.test('startsWith a prefix', function () {
    fc.assert(fc.property(fc.string(), fc.string(), function (prefix, suffix) { return Strings.startsWith(prefix + suffix, prefix); }));
});
//# sourceMappingURL=EnsureLeadingTest.js.map