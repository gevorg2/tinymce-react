/* tslint:disable:no-unimported-promise */
import { Assert, UnitTest } from '@ephox/bedrock-client';
import { Testable } from '@ephox/dispute';
// TODO: move to bedrock
/**
 * Execute a test defined as a Promise.
 *
 * Useful for fast-check asyncProperty tests. e.g.:
 *
 * ```
 * promiseTest('', () => {
 *   return fc.assert(fc.asyncProperty(fc.integer(), (i) => {
 *     return new Promise((resolve, reject) => {
 *
 *     });
 *   }));
 * });
 * ```
 * @param name
 * @param f
 */
export var promiseTest = function (name, f) {
    UnitTest.asynctest(name, function (success, failure) {
        f().then(function () {
            success();
        }, failure);
    });
};
// TODO: move to bedrock
export var eqAsync = function (label, expected, actual, reject, testableA) {
    if (testableA === void 0) { testableA = Testable.tAny; }
    try {
        Assert.eq(label, expected, actual, testableA);
    }
    catch (e) {
        reject(e);
    }
};
//# sourceMappingURL=AsyncProps.js.map