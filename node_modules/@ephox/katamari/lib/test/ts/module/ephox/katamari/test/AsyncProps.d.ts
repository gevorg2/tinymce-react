import { Testable } from '@ephox/dispute';
declare type Testable<A> = Testable.Testable<A>;
/**
 * Execute a test defined as a Promise.
 *
 * Useful for fast-check asyncProperty tests. e.g.:
 *
 * ```
 * promiseTest('', () => {
 *   return fc.assert(fc.asyncProperty(fc.integer(), (i) => {
 *     return new Promise((resolve, reject) => {
 *
 *     });
 *   }));
 * });
 * ```
 * @param name
 * @param f
 */
export declare const promiseTest: <A>(name: string, f: () => Promise<A>) => void;
export declare const eqAsync: <A>(label: string, expected: A, actual: A, reject: (a: any) => void, testableA?: Testable<A>) => void;
export {};
//# sourceMappingURL=AsyncProps.d.ts.map