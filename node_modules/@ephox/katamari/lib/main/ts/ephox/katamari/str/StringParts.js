import { Option } from '../api/Option';
/** Return the first 'count' letters from 'str'.
-     *  e.g. first("abcde", 2) === "ab"
-     */
export var first = function (str, count) {
    return str.substr(0, count);
};
/** Return the last 'count' letters from 'str'.
*  e.g. last("abcde", 2) === "de"
*/
export var last = function (str, count) {
    return str.substr(str.length - count, str.length);
};
export var head = function (str) {
    return str === '' ? Option.none() : Option.some(str.substr(0, 1));
};
export var tail = function (str) {
    return str === '' ? Option.none() : Option.some(str.substring(1));
};
//# sourceMappingURL=StringParts.js.map