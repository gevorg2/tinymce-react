import { Assert } from '@ephox/bedrock-client';
import { tElement } from './ElementInstances';
import * as Node from 'ephox/sugar/api/node/Node';
import { Testable } from '@ephox/dispute';
import { KAssert } from '@ephox/katamari-assertions';
var tArray = Testable.tArray;
var checkOpt = function (expected, actual) {
    KAssert.eqOption('eq', expected, actual, tElement);
};
var checkList = function (expected, actual) {
    Assert.eq('eq', expected, actual, tArray(tElement));
};
var isName = function (name) { return function (x) { return Node.name(x) === name; }; };
export default {
    checkOpt: checkOpt,
    checkList: checkList,
    isName: isName,
};
//# sourceMappingURL=Checkers.js.map