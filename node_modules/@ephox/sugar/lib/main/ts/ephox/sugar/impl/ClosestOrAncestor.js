import { Option, Type } from '@ephox/katamari';
export default function (is, ancestor, scope, a, isRoot) {
    return is(scope, a) ?
        Option.some(scope) :
        Type.isFunction(isRoot) && isRoot(scope) ?
            Option.none() :
            ancestor(scope, a, isRoot);
}
//# sourceMappingURL=ClosestOrAncestor.js.map