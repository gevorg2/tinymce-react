import { getTinymce } from './Globals';
import { document } from '@ephox/dom-globals';
var setTinymceBaseUrl = function (tinymce, baseUrl) {
    var prefix = document.location.protocol + '//' + document.location.host;
    tinymce.baseURL = baseUrl.indexOf('://') === -1 ? prefix + baseUrl : baseUrl;
    tinymce.baseURI = new tinymce.util.URI(tinymce.baseURL);
};
var updateTinymceUrls = function (packageName) {
    getTinymce().each(function (tinymce) {
        setTinymceBaseUrl(tinymce, "/project/node_modules/" + packageName);
    });
};
export { setTinymceBaseUrl, updateTinymceUrls };
//# sourceMappingURL=Urls.js.map