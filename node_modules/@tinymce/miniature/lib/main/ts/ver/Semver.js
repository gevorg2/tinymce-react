var toInt = function (str) {
    return parseInt(str, 10);
};
var cmp = function (a, b) {
    var delta = a - b;
    if (delta === 0) {
        return 0 /* EQ */;
    }
    return delta > 0 ? 1 /* GT */ : -1 /* LT */;
};
export var nu = function (major, minor, patch) {
    return { major: major, minor: minor, patch: patch };
};
export var parse = function (versionString) {
    var parts = /([0-9]+)\.([0-9]+)\.([0-9]+)(?:(\-.+)?)/.exec(versionString);
    return parts ? nu(toInt(parts[1]), toInt(parts[2]), toInt(parts[3])) : nu(0, 0, 0);
};
export var compare = function (version1, version2) {
    var cmp1 = cmp(version1.major, version2.major);
    if (cmp1 !== 0 /* EQ */) {
        return cmp1;
    }
    var cmp2 = cmp(version1.minor, version2.minor);
    if (cmp2 !== 0 /* EQ */) {
        return cmp2;
    }
    var cmp3 = cmp(version1.patch, version2.patch);
    if (cmp3 !== 0 /* EQ */) {
        return cmp3;
    }
    return 0 /* EQ */;
};
//# sourceMappingURL=Semver.js.map