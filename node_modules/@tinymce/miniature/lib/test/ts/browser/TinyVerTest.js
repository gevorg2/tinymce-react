import { UnitTest, assert } from '@ephox/bedrock-client';
import { TinyVer } from '../../../main/ts/api/Main';
UnitTest.test('TinyVerTest', function () {
    var assertgetVersion = function (expected, tiny) {
        assert.eq(expected, TinyVer.getVersion(tiny));
    };
    var fakeTiny = function (majorVersion, minorVersion) { return ({ majorVersion: majorVersion, minorVersion: minorVersion, plugins: {} }); };
    var v = function (major, minor, patch) { return ({ major: major, minor: minor, patch: patch }); };
    assertgetVersion(v(1, 2, 3), fakeTiny('1', '2.3'));
    assertgetVersion(v(1, 2, 3), fakeTiny('1', '2.3.4.5.6'));
    assertgetVersion(v(0, 0, 0), fakeTiny('arne', 'bertil.tommy'));
    assert.eq(true, TinyVer.isLessThan(fakeTiny('4', '5.5'), '4.6.5'));
    assert.eq(true, TinyVer.isGreaterThan(fakeTiny('4', '5.5'), '4.4.5'));
    assert.eq(false, TinyVer.isGreaterThan(null, '4.5.5'));
    assert.eq(false, TinyVer.isLessThan(null, '4.5.5'));
});
//# sourceMappingURL=TinyVerTest.js.map