import { Step, Chain } from '@ephox/agar';
declare const assertVersion: (expectedMajor: number, expectedMinor: number) => void;
declare const sAssertVersion: (expectedMajor: number, expectedMinor: number) => Step<unknown, unknown>;
declare const cAssertEditorVersion: (expectedMajor: number, expectedMinor: number) => Chain<any, any>;
export { assertVersion, sAssertVersion, cAssertEditorVersion };
